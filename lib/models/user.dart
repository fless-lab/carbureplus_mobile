class User {
  String nom, prenom, photoProfile;
  int id, stationId, compagnieId;

  User(
      {required this.nom,
      required this.prenom,
      required this.photoProfile,
      required this.id,
      required this.stationId,
      required this.compagnieId});
}
